这里为你设计并实现一套完整、模块化、高保真、纯前端自闭环运行的 **AI 全球业务增长系统（AI Global Growth OS）** 官网。代码采用 **React + TypeScript + Tailwind CSS + Lucide Icons + Framer Motion 动画风格**，无需外部图片资源，全部使用精细化 CSS/SVG 与动态仪表盘原生渲染。

---

### 项目文件结构速览

```text
src/
├── types/
│   └── index.ts                 # 全局数据模型
├── data/
│   └── mockData.ts              # 集中式 Demo 数据与业务流配置
├── components/
│   ├── ui/
│   │   ├── Badge.tsx            # 终端风胶囊标签
│   │   ├── GlowCard.tsx         # 极细发光边框卡片
│   │   └── TerminalWindow.tsx   # 仪表盘控制台外壳
│   ├── Navbar.tsx               # 极简毛玻璃导航栏
│   ├── Hero.tsx                 # 第一屏：Command Center 实时监控
│   ├── PainPoints.tsx           # 痛点与断裂链路
│   ├── CoreCapabilities.tsx     # 6大核心能力总容器
│   │   ├── LeadGeneration.tsx   # 01 获客
│   │   ├── Qualification.tsx    # 02 筛选
│   │   ├── SocialMedia.tsx      # 03 社媒矩阵
│   │   ├── CustomerSuccess.tsx  # 04 客户维护
│   │   ├── MarketIntel.tsx      # 05 国际市场研判
│   │   └── OrderManagement.tsx  # 06 订单与履约
│   ├── GrowthLoop.tsx           # 核心全景闭环流程图
│   ├── AIAgentLive.tsx          # AI 实时自治调度流
│   ├── Comparison.tsx           # 为什么是我们（从工具到运行）
│   ├── ResultsKpi.tsx           # 增长数据结果
│   ├── FinalCTA.tsx             # 底部强行动召唤
│   └── Footer.tsx               # 极简页脚
├── App.tsx                      # 主入口页面
└── index.css                    # 现代暗色微网格与动效样式

```

---

### 1. `src/types/index.ts`

```typescript
export interface LeadItem {
  id: string;
  company: string;
  country: string;
  industry: string;
  score: number;
  level: 'HIGH' | 'MEDIUM' | 'LOW';
  signal: string;
  matchRate: number;
}

export interface MarketAlert {
  id: string;
  title: string;
  category: 'TARIFF' | 'SUPPLY_CHAIN' | 'EXCHANGE' | 'POLICY';
  impactProduct: string;
  costImpact: string;
  demandImpact: string;
  competitiveness: string;
  recommendations: string[];
}

export interface OrderItem {
  id: string;
  customer: string;
  amount: string;
  progress: number;
  paymentStatus: string;
  logisticsStatus: string;
  eta: string;
  riskAlert?: string;
}

export interface AgentLog {
  id: string;
  time: string;
  content: string;
  status: 'done' | 'processing' | 'alert';
}

```

---

### 2. `src/data/mockData.ts`

```typescript
import { LeadItem, MarketAlert, OrderItem, AgentLog } from '../types';

export const INITIAL_COMMAND_CENTER = {
  leads: 1284,
  qualified: 326,
  opportunities: 47,
  orders: 12,
  pipeline: 284000,
};

export const MOCK_LEADS: LeadItem[] = [
  { id: 'LD-801', company: 'Nordic Lightings AB', country: 'SE', industry: 'Commercial Luminaire', score: 94, level: 'HIGH', signal: 'RFQ Released on LinkedIn', matchRate: 98 },
  { id: 'LD-802', company: 'Apex Hardware Group', country: 'US', industry: 'Industrial Electrical', score: 87, level: 'HIGH', signal: 'Expanding Supply Chain to APAC', matchRate: 92 },
  { id: 'LD-803', company: 'Vanguard Retailers BV', country: 'NL', industry: 'Smart Lighting Distribution', score: 82, level: 'HIGH', signal: 'CEO Visited Canton Fair Portal', matchRate: 89 },
  { id: 'LD-804', company: 'Helios Architectural', country: 'DE', industry: 'Facade Systems', score: 71, level: 'MEDIUM', signal: 'Annual Vendor Audit', matchRate: 74 },
  { id: 'LD-805', company: 'Pacific Source Co.', country: 'AU', industry: 'Wholesale Trade', score: 42, level: 'LOW', signal: 'Generic Inquiry Form', matchRate: 46 },
];

export const MOCK_MARKET_ALERT: MarketAlert = {
  id: 'ALT-2026-08',
  title: 'US announces new tariff adjustment for Section 301 list',
  category: 'TARIFF',
  impactProduct: 'LED Commercial Lighting Systems',
  costImpact: '+7.2%',
  demandImpact: '-1.4%',
  competitiveness: '-3.8%',
  recommendations: [
    'Prioritize high-margin smart control models to offset margin squeeze',
    'Accelerate distributor outreach in Canada & Mexico corridors',
    'Adjust bonded warehouse FOB staging schedule by 14 calendar days',
    'Trigger automated email sequence offering DDP fixed rates'
  ]
};

export const MOCK_ORDER: OrderItem = {
  id: 'ORD-10482',
  customer: 'ABC Corp (North America)',
  amount: '$48,200',
  progress: 82,
  paymentStatus: '70% Paid (LC Confirmed)',
  logisticsStatus: 'Customs Clearance Prepared',
  eta: 'Sep 14',
  riskAlert: 'Production delay flagged: +4 days estimated variance'
};

export const INITIAL_AGENT_LOGS: AgentLog[] = [
  { id: '1', time: '10:42:18', content: 'Scanning US & EU commercial lighting distributors...', status: 'done' },
  { id: '2', time: '10:42:24', content: '428 target companies verified with custom compliance check.', status: 'done' },
  { id: '3', time: '10:43:02', content: '86 high-fit buyers identified & matched with product SKU-92.', status: 'done' },
  { id: '4', time: '10:43:18', content: '12 purchase intent signals detected via B2B trade platforms.', status: 'done' },
  { id: '5', time: '10:44:03', content: '214 personalized localized outreach dispatched in parallel.', status: 'done' },
  { id: '6', time: '10:44:21', content: '3 high-intent RFQ responses received. Auto-translated & parsed.', status: 'alert' },
  { id: '7', time: '10:44:23', content: 'Sales Director alerted. Meeting slot tentatively reserved.', status: 'processing' },
];

export const PAIN_POINTS = [
  { key: '获客', sub: '精准度断层', desc: '不知道真正的海外买家是谁，更无法预判谁正处于采购决策周期。' },
  { key: '社媒', sub: '内容无法常态化', desc: '平台碎片化（LinkedIn/X/Meta），缺乏具备本地商业语境的长期运营团队。' },
  { key: '筛选', sub: '销售精力空转', desc: '团队将70%的时间浪费在无效垃圾线索中，高净值询盘被严重延误。' },
  { key: '市场', sub: '研判滞后性', desc: '关税变化、汇率波动、原材料上涨，知晓时往往已被动吞噬了订单利润。' },
  { key: '维护', sub: '客户资产流失', desc: '线索与订单散落在各业务员聊天记录和本地表格中，没有生命周期复购机制。' },
  { key: '订单', sub: '履约链路失控', desc: '工厂、货代、报关行与客户多方割裂，生产与物流风险缺乏前置预警机制。' }
];

```

---

### 3. `src/components/ui/Badge.tsx`

```tsx
import React from 'react';

interface BadgeProps {
  children: React.ReactNode;
  variant?: 'cyan' | 'emerald' | 'amber' | 'muted';
  className?: string;
}

export const Badge: React.FC<BadgeProps> = ({ children, variant = 'cyan', className = '' }) => {
  const styles = {
    cyan: 'bg-cyan-950/60 border-cyan-500/30 text-cyan-400',
    emerald: 'bg-emerald-950/60 border-emerald-500/30 text-emerald-400',
    amber: 'bg-amber-950/60 border-amber-500/30 text-amber-400',
    muted: 'bg-zinc-900/80 border-zinc-800 text-zinc-400',
  };

  return (
    <span className={`inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-xs font-mono tracking-wide border ${styles[variant]} ${className}`}>
      {children}
    </span>
  );
};

```

---

### 4. `src/components/ui/GlowCard.tsx`

```tsx
import React from 'react';

interface GlowCardProps {
  children: React.ReactNode;
  className?: string;
  highlight?: boolean;
}

export const GlowCard: React.FC<GlowCardProps> = ({ children, className = '', highlight = false }) => {
  return (
    <div
      className={`relative rounded-xl bg-zinc-950/80 backdrop-blur-md border transition-all duration-300 ${
        highlight
          ? 'border-cyan-500/40 shadow-[0_0_30px_-5px_rgba(6,182,212,0.15)]'
          : 'border-zinc-800/80 hover:border-zinc-700/90 shadow-xl'
      } ${className}`}
    >
      <div className="absolute inset-x-0 top-0 h-px bg-gradient-to-r from-transparent via-white/10 to-transparent" />
      {children}
    </div>
  );
};

```

---

### 5. `src/components/ui/TerminalWindow.tsx`

```tsx
import React from 'react';

interface TerminalProps {
  title: string;
  statusBadge?: React.ReactNode;
  children: React.ReactNode;
  className?: string;
}

export const TerminalWindow: React.FC<TerminalProps> = ({
  title,
  statusBadge,
  children,
  className = '',
}) => {
  return (
    <div className={`rounded-xl border border-zinc-800/90 bg-[#08090C] overflow-hidden shadow-2xl ${className}`}>
      <div className="flex items-center justify-between px-4 py-3 bg-zinc-900/60 border-b border-zinc-800/80">
        <div className="flex items-center space-x-2">
          <div className="w-2.5 h-2.5 rounded-full bg-zinc-700" />
          <div className="w-2.5 h-2.5 rounded-full bg-zinc-700" />
          <div className="w-2.5 h-2.5 rounded-full bg-zinc-700" />
          <span className="ml-2 font-mono text-xs text-zinc-400 uppercase tracking-wider">
            {title}
          </span>
        </div>
        <div>{statusBadge}</div>
      </div>
      <div className="p-5 font-sans">{children}</div>
    </div>
  );
};

```

---

### 6. `src/components/Navbar.tsx`

```tsx
import React, { useState, useEffect } from 'react';
import { Globe, ArrowUpRight } from 'lucide-react';

export const Navbar: React.FC = () => {
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => setScrolled(window.scrollY > 20);
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  return (
    <header
      className={`fixed top-0 left-0 right-0 z-50 transition-all duration-300 ${
        scrolled
          ? 'bg-black/75 backdrop-blur-xl border-b border-zinc-800/80 py-3.5'
          : 'bg-transparent py-5'
      }`}
    >
      <div className="max-w-7xl mx-auto px-6 flex items-center justify-between">
        {/* Logo */}
        <div className="flex items-center gap-3 cursor-pointer">
          <div className="w-8 h-8 rounded-lg bg-gradient-to-tr from-cyan-600 to-sky-400 flex items-center justify-center p-0.5 shadow-[0_0_15px_rgba(6,182,212,0.4)]">
            <div className="w-full h-full bg-black rounded-[6px] flex items-center justify-center">
              <Globe className="w-4 h-4 text-cyan-400" />
            </div>
          </div>
          <div className="flex flex-col">
            <span className="font-semibold tracking-wider text-sm text-white uppercase font-mono">
              NEXUS GLOBAL
            </span>
            <span className="text-[10px] text-zinc-500 -mt-1 tracking-widest font-mono">
              GROWTH OS
            </span>
          </div>
        </div>

        {/* Links */}
        <nav className="hidden md:flex items-center space-x-8 text-xs font-mono text-zinc-400">
          <a href="#loop" className="hover:text-cyan-400 transition-colors">业务闭环</a>
          <a href="#capabilities" className="hover:text-cyan-400 transition-colors">全链路能力</a>
          <a href="#market-intel" className="hover:text-cyan-400 transition-colors">市场情报系统</a>
          <a href="#agent" className="hover:text-cyan-400 transition-colors">自治引擎</a>
          <a href="#results" className="hover:text-cyan-400 transition-colors">运行成效</a>
        </nav>

        {/* CTAs */}
        <div className="flex items-center space-x-3">
          <button className="hidden sm:inline-flex text-xs font-mono text-zinc-300 hover:text-white px-3.5 py-2 transition-colors">
            登录控制台
          </button>
          <a
            href="#cta"
            className="inline-flex items-center gap-1.5 text-xs font-mono font-medium px-4 py-2 rounded-lg bg-zinc-100 text-black hover:bg-cyan-400 hover:text-black transition-all duration-200 shadow-md"
          >
            <span>获取增长方案</span>
            <ArrowUpRight className="w-3.5 h-3.5" />
          </a>
        </div>
      </div>
    </header>
  );
};

```

---

### 7. `src/components/Hero.tsx`

```tsx
import React, { useState, useEffect } from 'react';
import { TerminalWindow } from './ui/TerminalWindow';
import { Badge } from './ui/Badge';
import { ArrowRight, CheckCircle2, RefreshCw } from 'lucide-react';
import { INITIAL_COMMAND_CENTER } from '../data/mockData';

export const Hero: React.FC = () => {
  const [stats, setStats] = useState(INITIAL_COMMAND_CENTER);

  // 极轻微的微小数据跳动，呈现 live 状态
  useEffect(() => {
    const interval = setInterval(() => {
      setStats((prev) => ({
        ...prev,
        leads: prev.leads + (Math.random() > 0.6 ? 1 : 0),
        pipeline: prev.pipeline + (Math.random() > 0.8 ? 2400 : 0),
      }));
    }, 4000);
    return () => clearInterval(interval);
  }, []);

  return (
    <section className="relative pt-32 pb-24 md:pt-40 md:pb-32 overflow-hidden bg-black">
      {/* 极简发光背景 */}
      <div className="absolute top-1/4 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[700px] h-[350px] bg-gradient-to-b from-cyan-950/20 via-sky-900/10 to-transparent blur-[120px] pointer-events-none" />
      <div className="absolute inset-0 bg-[radial-gradient(#1e293b_1px,transparent_1px)] [background-size:24px_24px] opacity-20 pointer-events-none" />

      <div className="max-w-7xl mx-auto px-6 relative z-10">
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-12 lg:gap-8 items-center">
          
          {/* 左侧价值主张 */}
          <div className="lg:col-span-6 space-y-7">
            <div>
              <Badge variant="cyan" className="mb-4">
                <span className="w-1.5 h-1.5 rounded-full bg-cyan-400 animate-pulse" />
                AI POWERED GLOBAL BUSINESS
              </Badge>
              <h1 className="text-4xl sm:text-5xl lg:text-6xl font-extrabold text-white tracking-tight leading-[1.15]">
                让 AI，<br />
                <span className="text-transparent bg-clip-text bg-gradient-to-r from-zinc-100 via-cyan-200 to-cyan-400">
                  替你跑完整个海外业务。
                </span>
              </h1>
            </div>

            <p className="text-base sm:text-lg text-zinc-400 leading-relaxed max-w-xl font-normal">
              从获客、客群筛选、社媒运营，到全球市场研判、客户维护与订单风险追踪。<br />
              <strong className="text-zinc-200 font-medium">一个闭环系统，持续驱动你的全球业务增长。</strong>
            </p>

            <div className="flex flex-col sm:flex-row items-stretch sm:items-center gap-4 pt-2">
              <a
                href="#cta"
                className="inline-flex items-center justify-center gap-2 px-6 py-3.5 rounded-lg bg-cyan-400 text-black font-semibold text-sm hover:bg-cyan-300 transition-all shadow-[0_0_25px_rgba(6,182,212,0.3)] cursor-pointer"
              >
                <span>免费获取增长方案</span>
                <ArrowRight className="w-4 h-4" />
              </a>
              <a
                href="#loop"
                className="inline-flex items-center justify-center gap-2 px-6 py-3.5 rounded-lg bg-zinc-900 border border-zinc-800 text-zinc-300 font-mono text-sm hover:bg-zinc-800 hover:text-white transition-all cursor-pointer"
              >
                <span>查看系统如何工作</span>
                <span className="text-cyan-400">→</span>
              </a>
            </div>

            <div className="pt-4 border-t border-zinc-900 flex items-center gap-6 text-xs text-zinc-500 font-mono">
              <span className="flex items-center gap-1.5">
                <CheckCircle2 className="w-3.5 h-3.5 text-cyan-400" /> 非单点 Chat 工具
              </span>
              <span className="flex items-center gap-1.5">
                <CheckCircle2 className="w-3.5 h-3.5 text-cyan-400" /> 全链路自动化执行
              </span>
              <span className="flex items-center gap-1.5">
                <CheckCircle2 className="w-3.5 h-3.5 text-cyan-400" /> 真实外贸业务驱动
              </span>
            </div>
          </div>

          {/* 右侧：Global Growth Command Center */}
          <div className="lg:col-span-6">
            <TerminalWindow
              title="GLOBAL GROWTH OS // COMMAND CENTER"
              statusBadge={
                <div className="flex items-center gap-2">
                  <span className="inline-block w-2 h-2 rounded-full bg-emerald-400 animate-ping" />
                  <span className="text-[11px] font-mono font-semibold text-emerald-400 tracking-wider">
                    SYSTEM LIVE
                  </span>
                </div>
              }
            >
              <div className="space-y-6">
                {/* 核心指标网格 */}
                <div className="grid grid-cols-3 gap-3">
                  <div className="p-3 bg-zinc-900/70 border border-zinc-800/80 rounded-lg">
                    <div className="text-[11px] font-mono text-zinc-400">NEW LEADS</div>
                    <div className="text-xl font-bold font-mono text-white mt-1">
                      {stats.leads.toLocaleString()}
                    </div>
                    <div className="text-[10px] text-cyan-400 font-mono mt-0.5">↑ 24% 今日新增</div>
                  </div>
                  <div className="p-3 bg-zinc-900/70 border border-zinc-800/80 rounded-lg">
                    <div className="text-[11px] font-mono text-zinc-400">QUALIFIED</div>
                    <div className="text-xl font-bold font-mono text-cyan-400 mt-1">
                      {stats.qualified}
                    </div>
                    <div className="text-[10px] text-zinc-500 font-mono mt-0.5">High Intent 85+</div>
                  </div>
                  <div className="p-3 bg-zinc-900/70 border border-zinc-800/80 rounded-lg">
                    <div className="text-[11px] font-mono text-zinc-400">PIPELINE VALUE</div>
                    <div className="text-xl font-bold font-mono text-emerald-400 mt-1">
                      ${(stats.pipeline / 1000).toFixed(0)}K
                    </div>
                    <div className="text-[10px] text-emerald-500/80 font-mono mt-0.5">12 Live Orders</div>
                  </div>
                </div>

                {/* AI 实时后台处理流 */}
                <div className="p-4 rounded-lg bg-black/60 border border-zinc-800/80 space-y-2.5 font-mono text-xs">
                  <div className="flex items-center justify-between text-[11px] text-zinc-400 pb-2 border-b border-zinc-900">
                    <span className="flex items-center gap-1.5 text-zinc-300">
                      <RefreshCw className="w-3 h-3 text-cyan-400 animate-spin" />
                      AUTONOMOUS AGENT ACTIVE
                    </span>
                    <span className="text-[10px] text-zinc-500">24/7 NON-STOP</span>
                  </div>

                  <div className="space-y-1.5 text-zinc-300">
                    <div className="flex items-center gap-2">
                      <span className="text-emerald-400">✓</span>
                      <span>Discovered 428 target buyers in North America</span>
                    </div>
                    <div className="flex items-center gap-2">
                      <span className="text-emerald-400">✓</span>
                      <span>Qualified 86 decision-makers with buying signals</span>
                    </div>
                    <div className="flex items-center gap-2">
                      <span className="text-emerald-400">✓</span>
                      <span>Dispatched 214 contextual B2B intro sequences</span>
                    </div>
                    <div className="flex items-center gap-2">
                      <span className="text-cyan-400">⚡</span>
                      <span>Detected 12 urgent quote requests & initiated triage</span>
                    </div>
                    <div className="flex items-center gap-2 text-amber-400">
                      <span>⚠</span>
                      <span>Flagged 3 container ETA variances in Hamburg port</span>
                    </div>
                  </div>

                  <div className="pt-2 text-[11px] text-zinc-500 flex items-center gap-2">
                    <span className="inline-block w-1.5 h-1.5 rounded-full bg-cyan-400 animate-pulse" />
                    <span>AI OS is continuously executing overseas operations...</span>
                  </div>
                </div>
              </div>
            </TerminalWindow>
          </div>

        </div>
      </div>
    </section>
  );
};

```

---

### 8. `src/components/PainPoints.tsx`

```tsx
import React from 'react';
import { PAIN_POINTS } from '../data/mockData';
import { GlowCard } from './ui/GlowCard';
import { Unplug } from 'lucide-react';

export const PainPoints: React.FC = () => {
  return (
    <section className="py-24 bg-[#050608] border-y border-zinc-900">
      <div className="max-w-7xl mx-auto px-6">
        <div className="max-w-3xl mb-16">
          <div className="text-xs font-mono text-cyan-400 uppercase tracking-widest mb-3">
            // THE FRAGMENTATION PROBLEM
          </div>
          <h2 className="text-3xl sm:text-4xl font-bold text-white tracking-tight leading-tight">
            你的海外业务，<br />
            不是没有机会。
          </h2>
          <p className="text-zinc-400 mt-4 text-base sm:text-lg">
            而是机会散落在获客表格、社媒后台、邮件记录与货代群聊中，
            <span className="text-zinc-200">没有一个系统真正把它们连接并持续运转起来。</span>
          </p>
        </div>

        {/* 6 大核心痛点 */}
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5">
          {PAIN_POINTS.map((item) => (
            <GlowCard key={item.key} className="p-6">
              <div className="flex items-center justify-between mb-4">
                <span className="text-xs font-mono px-2 py-0.5 rounded bg-zinc-900 border border-zinc-800 text-cyan-400">
                  {item.key}
                </span>
                <span className="text-[11px] font-mono text-zinc-500 tracking-wider">
                  DISCONNECTED
                </span>
              </div>
              <h3 className="text-lg font-semibold text-zinc-100 mb-2">{item.sub}</h3>
              <p className="text-sm text-zinc-400 leading-relaxed">{item.desc}</p>
            </GlowCard>
          ))}
        </div>

        {/* 底部收敛总结 */}
        <div className="mt-12 p-6 rounded-xl bg-gradient-to-r from-zinc-950 via-zinc-900 to-zinc-950 border border-zinc-800/80 flex flex-col md:flex-row items-center justify-between gap-4">
          <div className="flex items-center gap-3">
            <div className="p-2.5 rounded-lg bg-cyan-950 border border-cyan-500/30 text-cyan-400">
              <Unplug className="w-5 h-5" />
            </div>
            <div>
              <div className="text-white font-medium text-sm">割裂导致巨大损耗</div>
              <div className="text-zinc-400 text-xs">销售找错人、跟进断档、政策失控、客户流失。</div>
            </div>
          </div>
          <div className="text-right">
            <span className="text-sm font-semibold text-cyan-400 font-mono">
              我们把这些散落的关键环节，放进同一个自主运转的系统 →
            </span>
          </div>
        </div>
      </div>
    </section>
  );
};

```

---

### 9. 核心能力 6 大子组件实现

#### 9.1 `src/components/CoreCapabilities/LeadGeneration.tsx`

```tsx
import React from 'react';
import { GlowCard } from '../ui/GlowCard';
import { TerminalWindow } from '../ui/TerminalWindow';
import { Filter, UserCheck, Zap } from 'lucide-react';

export const LeadGeneration: React.FC = () => {
  return (
    <GlowCard className="p-8 lg:p-10">
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-4">
          <div className="font-mono text-xs text-cyan-400 font-semibold tracking-wider uppercase">
            01 / 精准获客引擎
          </div>
          <h3 className="text-2xl sm:text-3xl font-bold text-white tracking-tight">
            持续找到真正可能<br />购买你的高意向客户。
          </h3>
          <p className="text-zinc-400 text-sm leading-relaxed">
            不只是在公海中机械爬取联系方式，而是深度扫描全球关务进出口数据、企业财报、社交媒体招聘动态与技术更迭，精准捕捉当下正在寻求供应商的真实买家。
          </p>
          <div className="pt-2 flex items-center gap-4 text-xs font-mono text-zinc-300">
            <span className="flex items-center gap-1.5"><Zap className="w-3.5 h-3.5 text-cyan-400" /> 采购信号监听</span>
            <span className="flex items-center gap-1.5"><UserCheck className="w-3.5 h-3.5 text-cyan-400" /> 决策人直达</span>
          </div>
        </div>

        {/* 交互演示 */}
        <div className="lg:col-span-7">
          <TerminalWindow title="LEAD_PIPELINE_RESOLVER">
            <div className="grid grid-cols-3 gap-2 mb-4 text-center">
              <div className="bg-zinc-900/80 p-2.5 rounded border border-zinc-800">
                <div className="text-[10px] font-mono text-zinc-500 uppercase">Potential Pool</div>
                <div className="text-lg font-bold font-mono text-zinc-200 mt-0.5">12,483</div>
              </div>
              <div className="bg-zinc-900/80 p-2.5 rounded border border-zinc-800">
                <div className="text-[10px] font-mono text-zinc-500 uppercase">Qualified Leads</div>
                <div className="text-lg font-bold font-mono text-cyan-400 mt-0.5">2,184</div>
              </div>
              <div className="bg-cyan-950/40 p-2.5 rounded border border-cyan-500/30">
                <div className="text-[10px] font-mono text-cyan-400 uppercase">High Intent RFQ</div>
                <div className="text-lg font-bold font-mono text-emerald-400 mt-0.5">326</div>
              </div>
            </div>

            {/* 流程节点 */}
            <div className="p-3 bg-black/60 rounded border border-zinc-800 font-mono text-xs space-y-2">
              <div className="flex items-center justify-between text-zinc-400 pb-1 border-b border-zinc-900 text-[11px]">
                <span>TARGET MATCHING WORKFLOW</span>
                <span className="text-emerald-400">ACTIVE</span>
              </div>
              <div className="flex flex-wrap items-center gap-2 text-zinc-300 text-[11px]">
                <span className="px-2 py-1 bg-zinc-900 rounded">全球企业库</span>
                <span className="text-zinc-600">→</span>
                <span className="px-2 py-1 bg-zinc-900 rounded">行业细分</span>
                <span className="text-zinc-600">→</span>
                <span className="px-2 py-1 bg-zinc-900 rounded">关务进出口匹配</span>
                <span className="text-zinc-600">→</span>
                <span className="px-2 py-1 bg-cyan-950 text-cyan-300 border border-cyan-500/40 rounded font-semibold">精准触达</span>
              </div>
            </div>
          </TerminalWindow>
        </div>
      </div>
    </GlowCard>
  );
};

```

#### 9.2 `src/components/CoreCapabilities/Qualification.tsx`

```tsx
import React from 'react';
import { GlowCard } from '../ui/GlowCard';
import { MOCK_LEADS } from '../../data/mockData';

export const Qualification: React.FC = () => {
  return (
    <GlowCard className="p-8 lg:p-10">
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-4">
          <div className="font-mono text-xs text-cyan-400 font-semibold tracking-wider uppercase">
            02 / 客群智能评级
          </div>
          <h3 className="text-2xl sm:text-3xl font-bold text-white tracking-tight">
            不是找到更多客户，<br />
            而是找到更值得跟进的客户。
          </h3>
          <p className="text-zinc-400 text-sm leading-relaxed">
            AI 综合企业规模、产品匹配度、历史采购频次、决策链权限及现金流健康度，多维度计算 Lead Score。让销售团队将 100% 的精力集中于头部 20% 高价值交易。
          </p>
        </div>

        {/* 动态 Score 展示 */}
        <div className="lg:col-span-7 bg-black/60 rounded-xl border border-zinc-800 p-4 font-mono text-xs">
          <div className="flex items-center justify-between text-zinc-400 pb-2.5 mb-2.5 border-b border-zinc-900 text-[11px]">
            <span>BUYER FIT & INTENT RANKING</span>
            <span>AUTO-SCORING ENGINE</span>
          </div>

          <div className="space-y-2">
            {MOCK_LEADS.map((lead) => (
              <div
                key={lead.id}
                className="flex items-center justify-between p-2.5 rounded bg-zinc-900/50 hover:bg-zinc-900 border border-zinc-800/80 transition-colors"
              >
                <div className="flex items-center gap-3">
                  <span className="w-5 text-zinc-500 font-bold">{lead.country}</span>
                  <div>
                    <div className="text-zinc-200 font-sans font-medium text-xs">{lead.company}</div>
                    <div className="text-[10px] text-zinc-500">{lead.signal}</div>
                  </div>
                </div>

                <div className="flex items-center gap-4">
                  <div className="text-right">
                    <span
                      className={`font-bold font-mono text-xs px-2 py-0.5 rounded ${
                        lead.score >= 85
                          ? 'bg-emerald-950 text-emerald-400 border border-emerald-500/40'
                          : lead.score >= 70
                          ? 'bg-cyan-950 text-cyan-400 border border-cyan-500/40'
                          : 'bg-zinc-900 text-zinc-500 border border-zinc-800'
                      }`}
                    >
                      SCORE {lead.score}
                    </span>
                  </div>
                  <span className="text-[10px] text-zinc-400 w-12 text-right">{lead.level}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </GlowCard>
  );
};

```

#### 9.3 `src/components/CoreCapabilities/SocialMedia.tsx`

```tsx
import React from 'react';
import { GlowCard } from '../ui/GlowCard';
import { Share2, Repeat, TrendingUp } from 'lucide-react';

export const SocialMedia: React.FC = () => {
  return (
    <GlowCard className="p-8 lg:p-10">
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-4">
          <div className="font-mono text-xs text-cyan-400 font-semibold tracking-wider uppercase">
            03 / 全球社媒矩阵运营
          </div>
          <h3 className="text-2xl sm:text-3xl font-bold text-white tracking-tight">
            让你的品牌，<br />
            持续出现在目标客户眼前。
          </h3>
          <p className="text-zinc-400 text-sm leading-relaxed">
            告别机械廉价的 AI 灌水文。系统结合当地工业热点与行业趋势，自动适配 LinkedIn、X、YouTube、Meta 的传播规范，形成具备专业技术说服力的持续品牌资产。
          </p>
          <div className="flex items-center gap-3 pt-2 text-xs font-mono text-zinc-400">
            <span className="px-2 py-1 bg-zinc-900 rounded border border-zinc-800">LinkedIn B2B</span>
            <span className="px-2 py-1 bg-zinc-900 rounded border border-zinc-800">X Tech Wire</span>
            <span className="px-2 py-1 bg-zinc-900 rounded border border-zinc-800">YouTube Case Studies</span>
          </div>
        </div>

        {/* 矩阵流控制台 */}
        <div className="lg:col-span-7 bg-black/60 rounded-xl border border-zinc-800 p-5 font-mono text-xs">
          <div className="flex items-center justify-between text-[11px] text-zinc-400 mb-3 pb-2 border-b border-zinc-900">
            <span className="flex items-center gap-1.5 text-zinc-300">
              <Share2 className="w-3.5 h-3.5 text-cyan-400" />
              GLOBAL CONTENT DISPATCH PIPELINE
            </span>
            <span className="text-emerald-400">STATUS: ACTIVE SCHEDULE</span>
          </div>

          <div className="space-y-3">
            <div className="p-3 bg-zinc-900/60 rounded border border-zinc-800 flex items-start justify-between gap-4">
              <div>
                <div className="text-zinc-300 font-sans font-medium text-xs">
                  "How High-Efficiency Industrial Optics Beat European ESG Benchmarks"
                </div>
                <div className="text-[10px] text-zinc-500 mt-1 font-mono">
                  Channels: LinkedIn Corporate + X Tech Thread • Target: DACH Region Buyers
                </div>
              </div>
              <span className="text-[10px] font-mono px-2 py-0.5 rounded bg-cyan-950 text-cyan-300 border border-cyan-500/30 whitespace-nowrap">
                Scheduled 14:00 CET
              </span>
            </div>

            <div className="p-3 bg-zinc-900/40 rounded border border-zinc-800 flex items-start justify-between gap-4">
              <div>
                <div className="text-zinc-300 font-sans font-medium text-xs">
                  "Factory Floor Video Walkthrough: UL/CE Cert Quality Check Series"
                </div>
                <div className="text-[10px] text-zinc-500 mt-1 font-mono">
                  Channels: YouTube Shorts + Meta B2B • Target: North America Importers
                </div>
              </div>
              <span className="text-[10px] font-mono px-2 py-0.5 rounded bg-emerald-950 text-emerald-400 border border-emerald-500/30 whitespace-nowrap">
                Engaged: 14 RFQs
              </span>
            </div>
          </div>
        </div>
      </div>
    </GlowCard>
  );
};

```

#### 9.4 `src/components/CoreCapabilities/CustomerSuccess.tsx`

```tsx
import React from 'react';
import { GlowCard } from '../ui/GlowCard';
import { Clock, RefreshCcw, BellRing } from 'lucide-react';

export const CustomerSuccess: React.FC = () => {
  return (
    <GlowCard className="p-8 lg:p-10">
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-4">
          <div className="font-mono text-xs text-cyan-400 font-semibold tracking-wider uppercase">
            04 / 客户生命周期维护
          </div>
          <h3 className="text-2xl sm:text-3xl font-bold text-white tracking-tight">
            成交不是终点，<br />
            而是客户生命周期的开始。
          </h3>
          <p className="text-zinc-400 text-sm leading-relaxed">
            实时监控老客户采购周期、开箱验货反馈及季节性补货规律。当客户采购频次发生异常下降或出现流失迹象时，自动向客户经理派发定制化的跟进策略。
          </p>
        </div>

        {/* 客户流失与复购警报卡片 */}
        <div className="lg:col-span-7 bg-[#090A0E] rounded-xl border border-zinc-800 p-5">
          <div className="flex items-center justify-between text-xs font-mono text-zinc-400 pb-3 border-b border-zinc-900">
            <span className="flex items-center gap-2 text-zinc-200">
              <BellRing className="w-3.5 h-3.5 text-amber-400" />
              CUSTOMER CHURN RISK & REPEAT ALERT
            </span>
            <span className="text-zinc-500">ACCOUNT: ABC CORP</span>
          </div>

          <div className="mt-4 grid grid-cols-1 sm:grid-cols-2 gap-4 font-mono text-xs">
            <div className="p-3.5 bg-black/60 rounded-lg border border-zinc-800/80 space-y-2">
              <div className="text-zinc-400 text-[11px]">METRICS SNAPSHOT</div>
              <div className="flex justify-between py-1 border-b border-zinc-900">
                <span className="text-zinc-500">Last Contact</span>
                <span className="text-zinc-200">18 days ago</span>
              </div>
              <div className="flex justify-between py-1 border-b border-zinc-900">
                <span className="text-zinc-500">Re-order Cycle</span>
                <span className="text-amber-400 font-semibold">Delayed (-32%)</span>
              </div>
              <div className="flex justify-between pt-1">
                <span className="text-zinc-500">LTV Value</span>
                <span className="text-emerald-400 font-bold">$142,000</span>
              </div>
            </div>

            <div className="p-3.5 bg-cyan-950/20 rounded-lg border border-cyan-500/30 flex flex-col justify-between">
              <div>
                <div className="text-[11px] font-mono text-cyan-400 font-semibold mb-1">
                  AI RECOMMENDATION
                </div>
                <p className="text-xs font-sans text-zinc-300 leading-normal">
                  "检测到其竞品发布 Q3 促销，建议本周主动提供定制化阶梯折扣方案与新品免费样品。"
                </p>
              </div>
              <div className="pt-2 text-[10px] font-mono text-cyan-400/80 flex items-center gap-1.5">
                <RefreshCcw className="w-3 h-3" /> Auto-drafting email template...
              </div>
            </div>
          </div>
        </div>
      </div>
    </GlowCard>
  );
};

```

#### 9.5 `src/components/CoreCapabilities/MarketIntel.tsx`

```tsx
import React from 'react';
import { GlowCard } from '../ui/GlowCard';
import { MOCK_MARKET_ALERT } from '../../data/mockData';
import { ShieldAlert, TrendingDown, ArrowUpRight, Compass } from 'lucide-react';

export const MarketIntel: React.FC = () => {
  const alert = MOCK_MARKET_ALERT;

  return (
    <GlowCard id="market-intel" className="p-8 lg:p-10" highlight>
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-4">
          <div className="font-mono text-xs text-cyan-400 font-semibold tracking-wider uppercase flex items-center gap-1.5">
            <Compass className="w-3.5 h-3.5" />
            05 / 国际形势与全球市场研判
          </div>
          <h3 className="text-2xl sm:text-3xl font-bold text-white tracking-tight">
            世界正在发生什么，<br />
            AI 告诉你它与业务的直接关系。
          </h3>
          <p className="text-zinc-300 text-sm leading-relaxed">
            不是单纯抓取泛资讯。系统 24 小时监控关税、汇率、地缘政策、原材料大宗商品价格与航运运力指数，
            <strong className="text-cyan-400 font-medium"> 核心在于：告诉你应该怎么办。</strong>
          </p>
          <div className="p-3 bg-zinc-900/60 rounded border border-zinc-800 text-xs text-zinc-400 font-mono">
            监控范围：国际贸易政策 • 关税变动 • 汇率波动 • 海运集装箱指数 • 供应链变动
          </div>
        </div>

        {/* 研判面板 */}
        <div className="lg:col-span-7 bg-[#08090C] rounded-xl border border-zinc-800 p-5 font-mono text-xs">
          {/* 警报头部 */}
          <div className="flex items-center justify-between pb-3 mb-3 border-b border-zinc-800 text-[11px]">
            <div className="flex items-center gap-2 text-amber-400 font-semibold">
              <ShieldAlert className="w-4 h-4" />
              <span>GLOBAL MARKET IMPACT ALERT</span>
            </div>
            <span className="text-zinc-500">ID: {alert.id}</span>
          </div>

          <div className="text-sm font-sans font-semibold text-zinc-100 mb-3">
            {alert.title}
          </div>

          {/* 影响评估矩阵 */}
          <div className="grid grid-cols-3 gap-2 mb-4">
            <div className="p-2.5 bg-black/60 rounded border border-zinc-800/80">
              <div className="text-[10px] text-zinc-500">Cost Impact</div>
              <div className="text-sm font-bold text-amber-400 mt-0.5">{alert.costImpact}</div>
            </div>
            <div className="p-2.5 bg-black/60 rounded border border-zinc-800/80">
              <div className="text-[10px] text-zinc-500">Demand Impact</div>
              <div className="text-sm font-bold text-cyan-400 mt-0.5">{alert.demandImpact}</div>
            </div>
            <div className="p-2.5 bg-black/60 rounded border border-zinc-800/80">
              <div className="text-[10px] text-zinc-500">Competitiveness</div>
              <div className="text-sm font-bold text-rose-400 mt-0.5">{alert.competitiveness}</div>
            </div>
          </div>

          {/* AI 应对建议 */}
          <div className="p-3.5 bg-cyan-950/20 rounded border border-cyan-500/30">
            <div className="text-[11px] text-cyan-400 font-semibold mb-2 flex items-center gap-1.5">
              <span>EXECUTIVE AI ACTION STRATEGY</span>
            </div>
            <div className="space-y-1.5 font-sans text-xs text-zinc-300">
              {alert.recommendations.map((rec, i) => (
                <div key={i} className="flex items-start gap-2">
                  <span className="text-cyan-400 font-mono text-[11px] font-bold">0{i + 1}</span>
                  <span>{rec}</span>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </GlowCard>
  );
};

```

#### 9.6 `src/components/CoreCapabilities/OrderManagement.tsx`

```tsx
import React from 'react';
import { GlowCard } from '../ui/GlowCard';
import { MOCK_ORDER } from '../../data/mockData';
import { AlertTriangle, Check, PackageCheck, Truck } from 'lucide-react';

export const OrderManagement: React.FC = () => {
  const order = MOCK_ORDER;

  return (
    <GlowCard className="p-8 lg:p-10">
      <div className="grid grid-cols-1 lg:grid-cols-12 gap-8 items-center">
        <div className="lg:col-span-5 space-y-4">
          <div className="font-mono text-xs text-cyan-400 font-semibold tracking-wider uppercase">
            06 / 全流程履约与风险预警
          </div>
          <h3 className="text-2xl sm:text-3xl font-bold text-white tracking-tight">
            从第一个询盘，<br />
            一直跟踪到订单安全交付。
          </h3>
          <p className="text-zinc-400 text-sm leading-relaxed">
            打通工厂生产排期、港口物流运单与外汇结算。自动识别生产延期、付款信用异常及海关清关堵点，防止交付违约与汇率回款损失。
          </p>
        </div>

        {/* 订单仪表盘 */}
        <div className="lg:col-span-7 bg-[#090A0E] rounded-xl border border-zinc-800 p-5 font-mono text-xs">
          <div className="flex items-center justify-between pb-3 border-b border-zinc-900 text-[11px]">
            <span className="text-zinc-200 font-bold">{order.id} // {order.customer}</span>
            <span className="text-emerald-400 font-bold">{order.amount}</span>
          </div>

          <div className="my-4 space-y-3">
            {/* 生产进度条 */}
            <div>
              <div className="flex justify-between text-[11px] text-zinc-400 mb-1.5">
                <span>Production Progress</span>
                <span className="text-cyan-400 font-bold">{order.progress}% Completed</span>
              </div>
              <div className="w-full h-2 bg-zinc-900 rounded-full overflow-hidden">
                <div className="h-full bg-gradient-to-r from-cyan-500 to-sky-400 rounded-full" style={{ width: `${order.progress}%` }} />
              </div>
            </div>

            <div className="grid grid-cols-3 gap-2 pt-2 text-[11px]">
              <div className="p-2 bg-black rounded border border-zinc-800">
                <div className="text-zinc-500 text-[10px]">Payment</div>
                <div className="text-zinc-200 mt-0.5">{order.paymentStatus}</div>
              </div>
              <div className="p-2 bg-black rounded border border-zinc-800">
                <div className="text-zinc-500 text-[10px]">Logistics</div>
                <div className="text-zinc-200 mt-0.5">{order.logisticsStatus}</div>
              </div>
              <div className="p-2 bg-black rounded border border-zinc-800">
                <div className="text-zinc-500 text-[10px]">ETA</div>
                <div className="text-cyan-400 mt-0.5 font-bold">{order.eta}</div>
              </div>
            </div>

            {/* 风险预警条 */}
            <div className="p-2.5 rounded bg-amber-950/30 border border-amber-500/40 text-amber-300 flex items-center gap-2 text-[11px]">
              <AlertTriangle className="w-3.5 h-3.5 text-amber-400 flex-shrink-0" />
              <span>{order.riskAlert}</span>
            </div>
          </div>
        </div>
      </div>
    </GlowCard>
  );
};

```

---

### 10. `src/components/CoreCapabilities.tsx`

```tsx
import React from 'react';
import { LeadGeneration } from './CoreCapabilities/LeadGeneration';
import { Qualification } from './CoreCapabilities/Qualification';
import { SocialMedia } from './CoreCapabilities/SocialMedia';
import { CustomerSuccess } from './CoreCapabilities/CustomerSuccess';
import { MarketIntel } from './CoreCapabilities/MarketIntel';
import { OrderManagement } from './CoreCapabilities/OrderManagement';

export const CoreCapabilities: React.FC = () => {
  return (
    <section id="capabilities" className="py-24 bg-black relative">
      <div className="max-w-7xl mx-auto px-6">
        <div className="max-w-3xl mb-16">
          <div className="text-xs font-mono text-cyan-400 uppercase tracking-widest mb-3">
            // UNIFIED GLOBAL INFRASTRUCTURE
          </div>
          <h2 className="text-3xl sm:text-4xl lg:text-5xl font-extrabold text-white tracking-tight">
            一个系统，<br />
            覆盖你的整个海外业务。
          </h2>
          <p className="text-zinc-400 mt-4 text-base sm:text-lg">
            不是六个独立的孤岛工具，而是深度协同、数据互通的企业级海外业务增长体系。
          </p>
        </div>

        <div className="space-y-6">
          <LeadGeneration />
          <Qualification />
          <SocialMedia />
          <CustomerSuccess />
          <MarketIntel />
          <OrderManagement />
        </div>
      </div>
    </section>
  );
};

```

---

### 11. `src/components/GrowthLoop.tsx` (核心闭环业务图)

```tsx
import React, { useState } from 'react';
import { GlowCard } from './ui/GlowCard';
import { Repeat, ArrowRight, Activity, Database, Sparkles } from 'lucide-react';

export const GrowthLoop: React.FC = () => {
  const [activeNode, setActiveNode] = useState<number | null>(null);

  const loopSteps = [
    { num: '01', title: '市场情报', en: 'MARKET INTEL', desc: '监测关税、政策与行业采购热点' },
    { num: '02', title: '目标客群', en: 'TARGET AUDIENCE', desc: '锁定精准画像与重点国家赛道' },
    { num: '03', title: '寻找客户', en: 'LEAD GENERATION', desc: '多源扫描海关数据与企业决策层' },
    { num: '04', title: '客户筛选', en: 'QUALIFICATION', desc: 'AI Intent Scoring 剔除低效线索' },
    { num: '05', title: 'AI 触达', en: 'OUTREACH', desc: '本地化高意图上下文邮件与社媒互动' },
    { num: '06', title: '客户沟通', en: 'CONVERSATION', desc: '实时多语种商业会话与意向解析' },
    { num: '07', title: '商机推进', en: 'OPPORTUNITY', desc: '技术参数核对与关键痛点化解' },
    { num: '08', title: '报价 / 样品', en: 'QUOTATION / SAMPLE', desc: '智能核算成本与样品寄送跟踪' },
    { num: '09', title: '订单交付', en: 'ORDER & DELIVERY', desc: '生产排期、港口物流与风控预警' },
    { num: '10', title: '客户维护', en: 'CUSTOMER SUCCESS', desc: '开箱满意度、补货周期智能监控' },
    { num: '11', title: '复购驱动', en: 'REPEAT PURCHASE', desc: '反哺市场数据，形成长期增长引擎' },
  ];

  return (
    <section id="loop" className="py-24 bg-[#060709] border-t border-zinc-900 relative overflow-hidden">
      <div className="max-w-7xl mx-auto px-6">
        <div className="text-center max-w-3xl mx-auto mb-16 space-y-4">
          <div className="text-xs font-mono text-cyan-400 uppercase tracking-widest">
            // END-TO-END BUSINESS LOOP
          </div>
          <h2 className="text-3xl sm:text-4xl lg:text-5xl font-extrabold text-white tracking-tight">
            这不是六个工具，<br />
            <span className="text-cyan-400">这是一个完整的增长闭环。</span>
          </h2>
          <p className="text-zinc-400 text-sm sm:text-base">
            每一次外贸触达、每一个样品反馈、每一张提单数据，都会自动转化为下一次精准获客与决策的燃料。
          </p>
        </div>

        {/* 循环图示容器 */}
        <div className="relative rounded-2xl bg-black/90 border border-zinc-800 p-6 lg:p-12 overflow-hidden shadow-2xl">
          {/* 中央发光引擎状态 */}
          <div className="flex flex-col items-center justify-center py-6 mb-8 text-center">
            <div className="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-cyan-950/80 border border-cyan-500/40 text-cyan-300 font-mono text-xs">
              <Sparkles className="w-3.5 h-3.5 text-cyan-400 animate-spin" />
              AI GLOBAL GROWTH ENGINE • CONTINUOUS LOOP
            </div>
          </div>

          {/* 节点网格 */}
          <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-6 gap-3">
            {loopSteps.slice(0, 6).map((step, idx) => (
              <div
                key={step.num}
                onMouseEnter={() => setActiveNode(idx)}
                onMouseLeave={() => setActiveNode(null)}
                className={`p-4 rounded-lg border transition-all cursor-pointer ${
                  activeNode === idx
                    ? 'bg-cyan-950/40 border-cyan-400 shadow-[0_0_15px_rgba(6,182,212,0.2)]'
                    : 'bg-zinc-900/60 border-zinc-800 hover:border-zinc-700'
                }`}
              >
                <div className="font-mono text-[11px] text-cyan-400 font-semibold">{step.num}</div>
                <div className="font-bold text-sm text-zinc-100 mt-1">{step.title}</div>
                <div className="text-[10px] font-mono text-zinc-500">{step.en}</div>
                <p className="text-[11px] text-zinc-400 mt-2 line-clamp-2">{step.desc}</p>
              </div>
            ))}
          </div>

          {/* 连接闭环指示器 */}
          <div className="flex items-center justify-center my-4">
            <div className="flex items-center gap-2 text-xs font-mono text-cyan-400/80 bg-zinc-900/80 px-4 py-1.5 rounded-full border border-zinc-800">
              <Repeat className="w-3.5 h-3.5 animate-spin" />
              <span>DATA FLOW & AUTOMATION PIPELINE</span>
            </div>
          </div>

          <div className="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-5 gap-3">
            {loopSteps.slice(6).map((step, idx) => {
              const actualIdx = idx + 6;
              return (
                <div
                  key={step.num}
                  onMouseEnter={() => setActiveNode(actualIdx)}
                  onMouseLeave={() => setActiveNode(null)}
                  className={`p-4 rounded-lg border transition-all cursor-pointer ${
                    activeNode === actualIdx
                      ? 'bg-cyan-950/40 border-cyan-400 shadow-[0_0_15px_rgba(6,182,212,0.2)]'
                      : 'bg-zinc-900/60 border-zinc-800 hover:border-zinc-700'
                  }`}
                >
                  <div className="font-mono text-[11px] text-cyan-400 font-semibold">{step.num}</div>
                  <div className="font-bold text-sm text-zinc-100 mt-1">{step.title}</div>
                  <div className="text-[10px] font-mono text-zinc-500">{step.en}</div>
                  <p className="text-[11px] text-zinc-400 mt-2 line-clamp-2">{step.desc}</p>
                </div>
              );
            })}
          </div>
        </div>
      </div>
    </section>
  );
};

```

---

### 12. `src/components/AIAgentLive.tsx` (AI 实时自治调度流)

```tsx
import React, { useState, useEffect } from 'react';
import { TerminalWindow } from './ui/TerminalWindow';
import { INITIAL_AGENT_LOGS } from '../data/mockData';
import { AgentLog } from '../types';
import { Bot, Terminal, ShieldCheck } from 'lucide-react';

export const AIAgentLive: React.FC = () => {
  const [logs, setLogs] = useState<AgentLog[]>(INITIAL_AGENT_LOGS);

  // 模拟终端实时新增日志
  useEffect(() => {
    const timer = setInterval(() => {
      const now = new Date();
      const timeStr = now.toTimeString().split(' ')[0];
      const newLog: AgentLog = {
        id: Date.now().toString(),
        time: timeStr,
        content: `Live Event: Scanned ${Math.floor(Math.random() * 20) + 10} customs manifest entries in Rotterdam Port.`,
        status: 'done',
      };
      setLogs((prev) => [newLog, ...prev.slice(0, 6)]);
    }, 6000);
    return () => clearInterval(timer);
  }, []);

  return (
    <section id="agent" className="py-24 bg-black border-t border-zinc-900">
      <div className="max-w-7xl mx-auto px-6">
        <div className="grid grid-cols-1 lg:grid-cols-12 gap-10 items-center">
          
          <div className="lg:col-span-5 space-y-5">
            <div className="text-xs font-mono text-cyan-400 uppercase tracking-widest">
              // AUTONOMOUS EXECUTION
            </div>
            <h2 className="text-3xl sm:text-4xl font-extrabold text-white tracking-tight leading-tight">
              你负责业务决策。<br />
              <span className="text-cyan-400">AI 负责全天候执行。</span>
            </h2>
            <p className="text-zinc-400 text-sm sm:text-base leading-relaxed">
              这不是一个需要你反复输入 Prompt 的聊天框。AI Agent 依据预设的全球增长策略，自主检索、筛选、沟通与调度，业务人员只需在关键节点一键确认。
            </p>
            <div className="space-y-2.5 pt-2 text-xs font-mono text-zinc-300">
              <div className="flex items-center gap-2">
                <ShieldCheck className="w-4 h-4 text-emerald-400" />
                <span>免培训：无需学习复杂模型指令与工具链</span>
              </div>
              <div className="flex items-center gap-2">
                <ShieldCheck className="w-4 h-4 text-emerald-400" />
                <span>免停滞：跨越时区限制，海外买家即时响应</span>
              </div>
            </div>
          </div>

          <div className="lg:col-span-7">
            <TerminalWindow
              title="AI_GROWTH_AGENT // REALTIME_EXEC_LOGS"
              statusBadge={
                <span className="font-mono text-xs text-cyan-400 flex items-center gap-1.5">
                  <span className="w-2 h-2 rounded-full bg-cyan-400 animate-pulse" />
                  LOGGING ACTIVE
                </span>
              }
            >
              <div className="space-y-2.5 font-mono text-xs max-h-[340px] overflow-y-auto pr-1">
                {logs.map((log) => (
                  <div
                    key={log.id}
                    className="flex items-start gap-3 p-2 rounded bg-zinc-900/40 border border-zinc-800/60"
                  >
                    <span className="text-zinc-500 font-mono text-[11px]">{log.time}</span>
                    <span
                      className={`text-[11px] ${
                        log.status === 'alert'
                          ? 'text-amber-400 font-bold'
                          : log.status === 'processing'
                          ? 'text-cyan-300'
                          : 'text-zinc-300'
                      }`}
                    >
                      {log.content}
                    </span>
                  </div>
                ))}
              </div>
            </TerminalWindow>
          </div>

        </div>
      </div>
    </section>
  );
};

```

---

### 13. `src/components/Comparison.tsx` (为什么是我们)

```tsx
import React from 'react';
import { GlowCard } from './ui/GlowCard';

export const Comparison: React.FC = () => {
  const comparisons = [
    {
      title: '从工具到结果',
      traditional: '传统 SaaS：卖给你账号和功能列表，让你自己招聘团队来操作。',
      nexus: '我们：直接替你运行整个海外获客与商机推进流程，向你交付确定性成果。',
    },
    {
      title: '从线索到订单',
      traditional: '传统获客：抓取一堆公开邮箱，线索导出后业务链路即宣告终结。',
      nexus: '我们：获客 → 筛选 → 触达 → 商机 → 样品 → 订单 → 复购全链路穿透。',
    },
    {
      title: '从信息到决策',
      traditional: '传统资讯：推送海量行业泛新闻，却无法量化对自身生意的具体影响。',
      nexus: '我们：不仅监测事件，更直接计算对你关税、成本与利润的冲击并给出行动建议。',
    },
    {
      title: '从订单到资产',
      traditional: '传统模式：业务员离职导致聊天记录与客户线索流失，每次重新开始。',
      nexus: '我们：沉淀企业全量业务数据大脑，每一次交易都在为下一次增长持续赋能。',
    },
  ];

  return (
    <section className="py-24 bg-[#050608] border-t border-zinc-900">
      <div className="max-w-7xl mx-auto px-6">
        <div className="max-w-3xl mb-16">
          <div className="text-xs font-mono text-cyan-400 uppercase tracking-widest mb-3">
            // PARADIGM SHIFT
          </div>
          <h2 className="text-3xl sm:text-4xl font-bold text-white tracking-tight">
            为什么这不是又一个 SaaS？
          </h2>
          <p className="text-zinc-400 mt-4 text-base">
            我们不是在向你推销复杂的软件工具，我们是在帮你重塑海外业务的运行方式。
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
          {comparisons.map((c, i) => (
            <GlowCard key={i} className="p-7 space-y-4">
              <h3 className="text-lg font-bold text-white font-mono flex items-center gap-2">
                <span className="text-cyan-400 text-sm">0{i + 1}.</span>
                {c.title}
              </h3>
              <div className="space-y-3 text-xs leading-relaxed">
                <div className="p-3 bg-zinc-900/50 rounded border border-zinc-800 text-zinc-400">
                  <span className="text-zinc-500 font-mono block mb-1 font-semibold">TRADITIONAL</span>
                  {c.traditional}
                </div>
                <div className="p-3 bg-cyan-950/20 rounded border border-cyan-500/30 text-zinc-200">
                  <span className="text-cyan-400 font-mono block mb-1 font-semibold">NEXUS GLOBAL OS</span>
                  {c.nexus}
                </div>
              </div>
            </GlowCard>
          ))}
        </div>
      </div>
    </section>
  );
};

```

---

### 14. `src/components/ResultsKpi.tsx`

```tsx
import React from 'react';
import { GlowCard } from './ui/GlowCard';
import { Badge } from './ui/Badge';

export const ResultsKpi: React.FC = () => {
  const kpis = [
    { label: 'New Leads', value: '+1,284', desc: '月度新增有效买家' },
    { label: 'Qualified Rate', value: '326', desc: '高意向决策人评级' },
    { label: 'Opportunities', value: '47', desc: '深度商机与样品递送' },
    { label: 'Quotations', value: '18', desc: '进入正式商务谈判' },
    { label: 'Confirmed Orders', value: '9', desc: '首期履约大货订单' },
    { label: 'Active Pipeline', value: '$284K', desc: '持续运转商机金额' },
  ];

  return (
    <section id="results" className="py-24 bg-black border-t border-zinc-900">
      <div className="max-w-7xl mx-auto px-6">
        <div className="flex flex-col md:flex-row md:items-end justify-between mb-16 gap-4">
          <div>
            <div className="text-xs font-mono text-cyan-400 uppercase tracking-widest mb-3">
              // BUSINESS OUTCOMES
            </div>
            <h2 className="text-3xl sm:text-4xl font-extrabold text-white tracking-tight">
              你最终得到的，<br />
              是一套可预测的全球业务增长。
            </h2>
          </div>
          <div>
            <Badge variant="muted">DEMO DATA • 某中型工业照明制造企业 60 天运行样本</Badge>
          </div>
        </div>

        <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
          {kpis.map((kpi, idx) => (
            <GlowCard key={idx} className="p-5 text-center">
              <div className="text-xs font-mono text-zinc-500 uppercase">{kpi.label}</div>
              <div className="text-2xl sm:text-3xl font-bold font-mono text-cyan-400 my-2">
                {kpi.value}
              </div>
              <div className="text-[11px] text-zinc-400">{kpi.desc}</div>
            </GlowCard>
          ))}
        </div>
      </div>
    </section>
  );
};

```

---

### 15. `src/components/FinalCTA.tsx`

```tsx
import React, { useState } from 'react';
import { ArrowRight, CheckCircle2, Sparkles } from 'lucide-react';

export const FinalCTA: React.FC = () => {
  const [product, setProduct] = useState('');
  const [targetMarket, setTargetMarket] = useState('');
  const [submitted, setSubmitted] = useState(false);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    if (!product || !targetMarket) return;
    setSubmitted(true);
  };

  return (
    <section id="cta" className="py-28 bg-[#040507] border-t border-zinc-800 relative overflow-hidden">
      {/* 底部环境光 */}
      <div className="absolute bottom-0 left-1/2 -translate-x-1/2 w-[800px] h-[300px] bg-cyan-950/20 blur-[140px] pointer-events-none" />

      <div className="max-w-4xl mx-auto px-6 relative z-10 text-center space-y-8">
        <div className="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-zinc-900 border border-zinc-800 text-xs font-mono text-zinc-400">
          <Sparkles className="w-3.5 h-3.5 text-cyan-400" />
          START YOUR GLOBAL ACCELERATION
        </div>

        <h2 className="text-3xl sm:text-5xl font-extrabold text-white tracking-tight leading-tight">
          让你的海外业务，<br />
          <span className="text-transparent bg-clip-text bg-gradient-to-r from-zinc-100 via-cyan-200 to-cyan-400">
            开始自己运转。
          </span>
        </h2>

        <p className="text-zinc-400 text-sm sm:text-base max-w-xl mx-auto">
          告诉我们你卖什么、卖给谁、想进入哪个目标市场。<br />
          系统将在 60 秒内为你生成第一份全球业务增长与市场进入方案。
        </p>

        {/* 方案生成输入交互 */}
        <div className="max-w-xl mx-auto">
          {submitted ? (
            <div className="p-6 rounded-xl bg-cyan-950/30 border border-cyan-500/40 text-left space-y-3 font-mono text-xs">
              <div className="flex items-center gap-2 text-emerald-400 font-bold text-sm">
                <CheckCircle2 className="w-4 h-4" />
                增长方案已生成并在后台初始化完成
              </div>
              <p className="text-zinc-300 font-sans">
                针对 <strong className="text-white">"{product}"</strong> 进军 <strong className="text-white">"{targetMarket}"</strong> 的市场研判报告与前 50 家高意向买家画像已准备就绪。
              </p>
              <div className="text-[11px] text-cyan-400 pt-2 border-t border-zinc-800">
                专属客户顾问将在 2 小时内与你建立安全加密链路对接。
              </div>
            </div>
          ) : (
            <form onSubmit={handleSubmit} className="space-y-3">
              <div className="grid grid-cols-1 sm:grid-cols-2 gap-3">
                <input
                  type="text"
                  placeholder="主营产品 (例: 商用储能电池)"
                  value={product}
                  onChange={(e) => setProduct(e.target.value)}
                  className="w-full px-4 py-3.5 rounded-lg bg-zinc-900 border border-zinc-800 text-zinc-200 text-sm focus:outline-none focus:border-cyan-400 transition-colors placeholder:text-zinc-600 font-sans"
                  required
                />
                <input
                  type="text"
                  placeholder="目标市场 (例: 东南亚 / 欧洲)"
                  value={targetMarket}
                  onChange={(e) => setTargetMarket(e.target.value)}
                  className="w-full px-4 py-3.5 rounded-lg bg-zinc-900 border border-zinc-800 text-zinc-200 text-sm focus:outline-none focus:border-cyan-400 transition-colors placeholder:text-zinc-600 font-sans"
                  required
                />
              </div>
              <button
                type="submit"
                className="w-full py-4 rounded-lg bg-cyan-400 hover:bg-cyan-300 text-black font-bold text-sm transition-all flex items-center justify-center gap-2 shadow-[0_0_30px_rgba(6,182,212,0.3)] cursor-pointer"
              >
                <span>获取我的专属全球增长方案</span>
                <ArrowRight className="w-4 h-4" />
              </button>
            </form>
          )}
        </div>

        <div className="flex flex-wrap items-center justify-center gap-6 text-xs text-zinc-500 font-mono pt-4">
          <span>✓ 无需复杂部署</span>
          <span>✓ 无需学习繁琐工具</span>
          <span>✓ 从你的第一条核心业务线开始运转</span>
        </div>
      </div>
    </section>
  );
};

```

---

### 16. `src/components/Footer.tsx`

```tsx
import React from 'react';
import { Globe } from 'lucide-react';

export const Footer: React.FC = () => {
  return (
    <footer className="py-12 bg-black border-t border-zinc-900 text-zinc-500 text-xs font-mono">
      <div className="max-w-7xl mx-auto px-6 flex flex-col sm:flex-row items-center justify-between gap-6">
        <div className="flex items-center gap-2 text-zinc-300 font-semibold">
          <Globe className="w-4 h-4 text-cyan-400" />
          <span>NEXUS GLOBAL OS</span>
          <span className="text-zinc-600 text-[10px]">© 2026</span>
        </div>

        <div className="flex items-center space-x-6 text-zinc-400">
          <a href="#loop" className="hover:text-cyan-400 transition-colors">业务闭环</a>
          <a href="#capabilities" className="hover:text-cyan-400 transition-colors">全景功能</a>
          <a href="#market-intel" className="hover:text-cyan-400 transition-colors">宏观研判</a>
          <a href="#cta" className="hover:text-cyan-400 transition-colors">获取方案</a>
        </div>

        <div className="text-zinc-600 text-[11px]">
          AI + DATA + AUTOMATION = ENTERPRISE GLOBAL EXPANSION
        </div>
      </div>
    </footer>
  );
};

```

---

### 17. `src/App.tsx` (整站组装入口)

```tsx
import React from 'react';
import { Navbar } from './components/Navbar';
import { Hero } from './components/Hero';
import { PainPoints } from './components/PainPoints';
import { GrowthLoop } from './components/GrowthLoop';
import { CoreCapabilities } from './components/CoreCapabilities';
import { AIAgentLive } from './components/AIAgentLive';
import { Comparison } from './components/Comparison';
import { ResultsKpi } from './components/ResultsKpi';
import { FinalCTA } from './components/FinalCTA';
import { Footer } from './components/Footer';

export const App: React.FC = () => {
  return (
    <div className="min-h-screen bg-black text-zinc-100 font-sans selection:bg-cyan-500 selection:text-black">
      <Navbar />
      <main>
        <Hero />
        <PainPoints />
        <GrowthLoop />
        <CoreCapabilities />
        <AIAgentLive />
        <Comparison />
        <ResultsKpi />
        <FinalCTA />
      </main>
      <Footer />
    </div>
  );
};

export default App;

```

---

### 18. `src/index.css` (暗色现代极简风格)

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  body {
    @apply bg-black text-zinc-100 antialiased selection:bg-cyan-400 selection:text-black;
    font-feature-settings: "cv02", "cv03", "cv04", "cv11";
  }
}

/* 自定义暗色滚动条 */
::-webkit-scrollbar {
  width: 6px;
}
::-webkit-scrollbar-track {
  background: #000000;
}
::-webkit-scrollbar-thumb {
  background: #27272a;
  border-radius: 3px;
}
::-webkit-scrollbar-thumb:hover {
  background: #3f3f46;
}

```